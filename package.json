{
  "name": "jev-browser",
  "version": "0.1.0",
  "description": "Jev as the decision step, any browser harness as the hands. Zero runtime dependencies.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.6"
  },
  "bin": {
    "jev": "./src/cli.ts"
  },
  "exports": {
    ".": "./index.ts",
    "./core": "./src/core/index.ts"
  },
  "scripts": {
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@opencode/plugin": "^2.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
